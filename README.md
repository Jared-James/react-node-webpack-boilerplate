# react-node-webpack-boilerplate

For jumping into new projects that need React | Node | webpack | SASS

```bash
Npm run install
npm run build (This runs both the server and client code)

Sever runs on localhost:3000
client runs on localhost:8080

```
